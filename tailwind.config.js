/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}", // 👈 Tells Tailwind to scan your components
  ],
  theme: {
    extend: {
      // We'll put custom fonts, colors, etc. here later
    },
  },
  plugins: [],
};
